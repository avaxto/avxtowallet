// Stands in for image/font imports under Jest — see moduleNameMapper in
// jest.config.js. Vite turns these into URL strings; Jest would try to parse
// the binary.
module.exports = 'test-asset-stub'
